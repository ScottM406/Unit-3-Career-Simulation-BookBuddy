use login token in single book view to add checkout to single book view

add book of the month, and account pages to nav bar

fix css rules for navbar (it's stretching out the inputs in  login/register)